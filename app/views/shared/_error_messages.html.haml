- if target.errors.any?
	.errorExplanation
		%h2
			=t('activerecord.errors.template.header', :model => t("activerecord.models.#{User.to_s.downcase}"), :count => target.errors.count)
		%ul
			- target.errors.full_messages.each do |msg|
				%li= msg