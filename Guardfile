# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'mozrepl' do
  watch(/app\/.+\.(erb|haml|sass)/)
  watch(/app\/helpers\/.+\.rb/)
  watch(/config\/locales\/.+\.yml/)
end

### or with options...
# guard 'mozrepl', :host => 'localhost', :port => 4242, :verbose => true do
#   ...
